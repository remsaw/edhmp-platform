version: '3.8'

services:
  edhmp-app:
    build: .
    container_name: edhmp_app
    ports:
      - "8501:8501"
    restart: always
    environment:
      - STREAMLIT_SERVER_PORT=8501
      - STREAMLIT_SERVER_ADDRESS=0.0.0.0
